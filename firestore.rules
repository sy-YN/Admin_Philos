rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 開発用のルールです。すべてのユーザーからの読み書きを許可します。
    // 本番環境では、より厳格なルールに置き換える必要があります。
    // 例: allow read, write: if request.auth != null;
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
